import 'package:flutter/material.dart';

class ChallengePart2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('challenge part 2'),
      ),
      body: Container(
        child: Text('hello.'),
      ),
    );
  }
}
